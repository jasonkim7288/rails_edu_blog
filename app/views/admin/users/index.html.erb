<div class="main">
    <section>
        <h1>Manage Users <%= link_to 'Create New', new_admin_user_path %></h1>
        <% if @users.exists? %>
            <table>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Date Created</th>
                    <th>Actions</th>
                </tr>
                <% @users.each do |user| %>
                    <tr>
                        <td><%= user.name %></td>
                        <td><%= user.email %></td>
                        <td><%= user.created_at.to_time.strftime("%B %e at %l:%M %p") %></td>
                        <td><%= link_to 'Edit', edit_admin_user_path(user) %>       <%= link_to 'Delete', admin_user_path(user), method: :delete, data: {confirm: "Are you sure?"} %></td>
                    </tr>
                <% end %>
            </table>
        <% else %>
            <h2>There is no user</h2>
        <% end %>
    </section>
</div>